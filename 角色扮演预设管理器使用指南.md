# 角色扮演预设管理器 · 使用指南

> 入口：**设置 → 角色与对话 → 角色扮演预设管理器…（参数 / 规则卡 / 正则 / 世界书）**
> 打开即全屏窗口（标题栏可最小化 / 还原，底部也有「全屏/还原」按钮，快捷键 `F11`）。
> 数据落在 `data/roleplay_preset.json`，设定卡落在 `roleplay/` 目录。

---

## 0. 它到底是干什么的

主界面的 **「开启角色扮演」** 只是一种开关；真正决定「这一次对话怎么演」的是**预设**。
预设管理器就是把下面这些东西集中在一处编辑：

| 你想做的事 | 去哪一页 |
| --- | --- |
| 想让模型更发散 / 更保守、限制输出长度 | 采样参数 |
| 想给对话挂上文风、世界观、限制卡 | 设定卡 |
| 想追加一大段固定指令（思考链、格式要求） | 规则卡 |
| 想在发送/显示时自动改写文本 | 正则脚本 |
| 想让「提到某关键词时才注入某段设定」 | 世界书 |
| 想控制注入顺序、记忆隔离、输出骨架 | 装配与隔离 |

**重要**：所有修改都要点 **「保存并关闭」** 才会写入；改成什么样，**下一次对话**才生效（已发生的历史不会回溯）。

---

## 1. 采样参数（酒馆式）

### 是干什么的
覆盖本次角色扮演对话的模型采样参数。**没勾选「开」的项一律沿用全局 `config.json`**，所以你可以只接管温度、其它保持不动。

### 怎么用
1. 勾选顶部 **「启用采样参数覆盖」**（不勾 = 整页不生效，等于关掉）。
2. 在需要的项目上勾 **「开」**，调数值。
3. 保存。

### 各项速查

| 参数 | 作用 | 常用值 |
| --- | --- | --- |
| 温度 temperature | 越高越发散、越敢写；越低越保守 | 创作 0.9~1.1，严谨讨论 0.6~0.8 |
| 核采样 top_p | 只从累计概率前 p 的候选里挑 | 1.0 = 不截断；0.9 更稳 |
| 候选上限 top_k | 只保留概率最高的 k 个候选 | 0 = 关闭；64 左右更稳 |
| 最小概率 min_p | 去掉概率过低的尾部 | 0 = 关闭；0.05~0.1 可减少胡言乱语 |
| top_a | 动态截断，配合 min_p 使用 | 0 = 关闭 |
| 重复惩罚 repetition_penalty | >1 抑制复读 | 1.0 = 关闭，1.05~1.15 轻微抑制 |
| 频率/存在惩罚 | 正值降低重复用词 / 鼓励新话题 | 0.1~0.5 |
| 单次最大输出 max_tokens | 单轮回复上限 | 短对话 1024，长篇 8000+ |
| 上下文上限 max_context | 提示性数值 | 32768 |
| 随机种子 seed | 固定后结果可复现 | -1 = 每次随机 |
| 推理强度 reasoning_effort | 仅推理模型有效 | 留空 = 不指定 |

---

## 2. 设定卡（对话风格 / 世界观 / 个人设定 / 角色工具）

### 是干什么的
设定卡是 `roleplay/` 目录下的 JSON 文件，**勾选后整份内容会原文注入系统提示词**。四类：

| 类别 | 目录 | 放什么 | 你现在的卡 |
| --- | --- | --- | --- |
| 对话风格 | `roleplay/rolemanger/style/` | 文风、口吻、篇幅、叙事方式 | 日常陪伴与聊天、日式轻小说文风、角色扮演短对话、认真严谨的讨论 |
| 世界观 | `roleplay/rolemanger/world/` | 世界背景、地理、种族、势力 | 小马国、小马国女孩世界、My Little Pony 世界书（导入） |
| 个人设定 | `roleplay/rolepersonal/` | 你自己（user）的人物卡、人物关系 | user、MLPship、EQGship |
| 角色工具 | `roleplay/roletools/` | 限制、破限、格式、功能开关型卡片 | anti-AIstyle、nextstep、removeEmoji 等 + 6 张导入的酒馆预设 |

### 怎么用
- 在对应分组里 **Ctrl + 单击多选**（点击即切换选中）。
- 想看某张卡写了什么：用 **角色对话管理 → 打开角色扮演管理器** 查看/编辑卡片原文。
- **同一时间建议只勾一张「角色工具」预设卡**（如 yinbreak、浮生Vane），多张一起勾会互相打架。
- 风格卡 + 世界观卡 + 一张工具卡，是最常见组合。

### 关于导入进来的 6 张酒馆预设卡
来自你指定的预设目录，按内容自动归类：

| 原文件 | 归类 | 内容 |
| --- | --- | --- |
| `TGbreakV1.1.0` / `TGbreakV3.0.5` / `yinbreak` / `浮生Vane` / `潮汐Chaoxibreak` / `灰魂breakv1.6 pro` | **角色工具** | 提示词 29~47 条（仅源文件中开启的）、正则 5~20 条、采样参数 |
| `My Little Pony 世界书` | **世界观** | 18 个条目（触发词/常驻/概率/排序全保留）；同时在「世界书」页写了一份**默认停用**的触发式副本 |

导入时做了两处净化：
1. **剔除工具配置 dump**：`SPreset配置` 这类字段会把 SillyTavern 编辑器配置整段 JSON 塞进提示词（单段 3.4~5.4 万字），对模型毫无意义，已自动剔除。
2. **只保留源文件中处于启用状态的提示词**：避免把作者没启用的实验性条目一起注入。想要全部可用命令行 `--all` 重导。

> 体积提示：一张预设卡正文约 7 千~1.5 万字，勾选后会占用相应上下文。觉得太长就去 `roleplay/roletools/` 里直接编辑 JSON，删掉不需要的 `prompts` 条目。

---

## 3. 规则卡

### 是干什么的
按顺序追加到系统提示词里的**一段固定指令**。三种模式：

| 模式 | 作用 | 什么时候用 |
| --- | --- | --- |
| `text` | 原文直接输出 | 绝大多数情况：格式要求、思考链、禁止事项 |
| `setvar` | 把内容**写入**变量（覆盖） | 定义一段可复用的设定，如「本场角色的已知信息」 |
| `addvar` | 把内容**追加**到变量（累加） | 多选累加，如「额外禁止事项」 |

变量用 `{{getvar::变量名}}` 消费 —— **定义与消费分离**：规则卡负责定义，输出骨架/设定卡负责消费。
排序用 ↑ ↓ 调整（越靠前越先注入）。

### 怎么用
左侧选一条 → 右侧编辑 → **「应用修改」**。
顶部「来源分组」可按导入来源过滤；**「启用本组 / 停用本组」** 按整组开关；**「启用/停用」** 切单条。

### 可用宏
`{{user}}` `{{char}}` `{{group}}` `{{members}}` `{{time}}` `{{date}}` `{{weekday}}` `{{datetime}}` `{{random::甲::乙::丙}}`

---

## 4. 正则脚本

### 是干什么的
在**发送前**或**收到后**自动改写文本，不用改角色卡。

- **作用位置**：用户消息（发送前）/ AI 回复（收到后）
- **通道**：
  - 都不勾 → 展示层和模型层都生效
  - 仅展示层(markdownOnly) → **只有你看到的效果被改**，模型/记忆看到的仍是原文
  - 仅模型层(promptOnly) → **只有送给模型的内容被改**，界面显示原文
  - 两者同时勾 = 配置错误，该条会被跳过
- **深度范围**：深度 0 = 最新一条消息；`min=0 / max=2` 表示只对最近三层生效；最大深度填 0 = 不限

### 怎么用
写法支持 `/正则/gi` 或裸正则；替换串里 `$1`、`${1}`、`$<name>` 引用分组，`$$` 表示字面量 `$`。

示例：把套话标出来让模型自己看见
- 匹配：`/(不是[^，]+，是[^。]+。)/g`
- 替换：`<cliche>$1</cliche>`

---

## 5. 世界书

### 是干什么的
**按关键词触发**才注入的设定，比设定卡省上下文。

| 字段 | 含义 |
| --- | --- |
| 主触发词 | 命中任意一个即视为命中 |
| 次触发词 | 必须与主触发词**同时出现**才算命中 |
| 常驻 | 不触发也一直注入 |
| 排序 | 越小越靠前 |
| 深度 | 注入到距最新多少层的位置（0 = 最新） |
| 概率% | 命中后按概率决定是否注入 |
| 插入位置 | 0 角色定义前 / 1 角色定义后 / 2 深度锚点后 |

扫描范围是「本轮用户消息 + 最近若干条对话」，命中即注入。

---

## 6. 装配与隔离

### 上下文装配
| 项 | 说明 |
| --- | --- |
| 注入最近对话轮数 | 把最近 N 轮对话一并装配进上下文 |
| 排他模式 | 角色扮演时**不注入技能/联网/技能包**，只保留角色与角色扮演工具（默认开） |
| 注入长期记忆 | 只注入「角色扮演命名空间」里的记忆（默认开） |
| 要求情绪标签 | 要求每句末尾带 `〖情绪标签〗`（默认开） |
| 注入日常作息 | 注入时间/作息上下文（默认关，避免出戏） |

### 对话隔离
- **角色扮演对话与日常对话互不读取**（建议常开）：角色扮演读不到日常记忆，日常也读不到扮演记忆；历史列表也按空间过滤。
- **每个对话独立**：开启后每个新会话各自一份短期/长期记忆；关闭则所有角色扮演会话共享一份（但仍与日常隔离）。

### 输出骨架
留空 = 不约束输出格式。填了之后模型输出会按骨架组织，可消费规则卡定义的变量，例如：

```
<draft_notes>{{getvar::COT-防全知}}</draft_notes>
{{getvar::正文}}
```

---

## 7. 导入酒馆（SillyTavern）文件

底部两个按钮：

**① 分类导入为设定卡…（推荐）**
选一个目录 → 自动识别并按类别落盘：

| 源文件类型 | 判定依据 | 归到哪一类 |
| --- | --- | --- |
| 世界书 | 顶层含 `entries` | 世界观 `world` |
| 酒馆预设 | 顶层含 `prompts`（破限/规则/正则/采样） | 角色工具 `roletools` |
| 风格类 | 文件名含 style/风格/文风/口吻/语气 | 对话风格 `style` |

同名文件覆盖；世界书会额外写一份默认停用的触发式条目到「世界书」页。

**② 导入为规则卡／正则／世界书…**
把同一批文件拆开塞进预设内部的三个列表（按来源分组，可批量启停）。适合想逐条精修的场景。

命令行等价用法：

```bash
# 分类导入为设定卡
python -m roleplay.roleplay_import "<预设目录>" --as-cards
# 只导入某一类 / 连未启用的提示词一起导入
python -m roleplay.roleplay_import <目录> --as-cards --category-tools --all
# 拆成规则卡/正则/世界书导入（可选连采样参数一起）
python -m roleplay.roleplay_import <目录> --with-sampling
```

---

## 8. 常见问题

**Q：改完了没变化？**
A：① 没点「保存并关闭」；② 主界面没开「开启角色扮演」；③ 改的项没勾「开」（采样参数）；④ 已发生的对话不会回溯，开新会话。

**Q：回复突然变长/变怪？**
A：检查是不是同时勾了多张「角色工具」预设卡，或采样参数被导入覆盖了 —— 去采样参数页关掉「启用采样参数覆盖」即可回到全局设置。

**Q：想回到出厂状态？**
A：底部「恢复默认」。

**Q：设定卡怎么编辑？**
A：直接改 `roleplay/` 下对应 JSON，或用「角色对话管理 → 打开角色扮演管理器」。

**Q：为什么界面和我主程序主题色一样？**
A：管理器会自动同步主界面主题色；它使用自包含样式，不受主窗口无边框/半透明样式影响。

---

## 9. 主角设定（你在这段扮演里的身份）

位置：**设置 → 角色对话管理 →「主角设定（你在这段扮演里的身份，单选）」**，
或预设管理器 **装配页 →「主角设定（你本人的身份卡，单选）」**（两处同步）。

- 下拉里列出的是 `roleplay/rolepersonal/` 下的个人设定卡，**单选**；
- 选中的卡会以 **【主角设定（{{user}} 本人）】** 单独一节注入，让模型知道「你」是谁，
  不会和角色（AI）的设定混在一起；
- 为避免同一张卡注入两遍：被选为主角的卡会**自动从「个人设定」多选里剔除**，
  其它个人设定卡照常注入；
- 不选（「不使用主角设定」）时行为与以前完全一致。

## 10. OOC / 突破世界观工具

**开关**：设置 → 角色对话管理 →「允许 OOC / 突破世界观（仍参考世界书背景设定）」
（预设管理器装配页同名开关，两处同步；保存在 `data/roleplay_preset.json` 的 `assembly.ooc_mode`）。

开启后会追加一段指令，含义是：

1. 允许 **OOC 出戏**：脱角色说明、吐槽、复盘、和用户讨论设定与走向（可用【OOC】标注）；
2. 允许 **突破世界观**：跨世界观联动、引入原作不存在的要素、打破第四面墙，
   不再因「设定里不可能发生」而拒绝；
3. 但仍**参考【世界书】/【世界观】的背景设定**：地名、势力、历史时间线、专有术语、
   人物关系优先沿用，只有偏离能提升张力或你明确要求时才偏离；
4. 角色性格基调与口吻不变，你要「回到角色 / 继续剧情」时立即恢复正常扮演。

**配套工具卡**：`roleplay/roletools/OOC与世界观破限.json`
（在设置面板「角色工具」分类里勾选即可加载更详细的破限规则；两张可同时用，也可只用其一）。

## 11. 长对话 / 长回复不再卡顿

针对「长回复很占资源甚至卡死闪退」做的处理（都在主界面渲染层，不改变记录内容）：

| 场景 | 处理 |
|---|---|
| 输出超过 8000 字 | 流式期间只渲染**尾部 4000 字**，前面提示「暂时省略」，完成后完整显示 |
| 输出超过 24000 字 | 流式期间改用**纯文本**渲染（Qt 富文本对超大文档极慢），完成后再一次性富文本 |
| 刷新频率 | 输出越长节流间隔越大：30ms → 120ms → 220ms（不再逐 token 重排整篇） |
| 思维链折叠框 | 长度变化 ≥400 字或间隔 ≥1 秒才刷新一次 |
| 最终长回复 | 气泡只显示**首尾摘要** + 「完整回复（N 字）」折叠块；完整原文仍在记录/存档里 |
| 聊天区控件 | 最多保留最近 80 条气泡，超出的旧控件自动释放（记录不受影响） |

实测：33000 字的完整富文本渲染约 189 ms/帧（30ms 一帧 → 必然假死），
优化后流式一帧约 25 ms、完成时约 14 ms。