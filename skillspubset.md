# skill库（skillspub 公共技能库）设置说明

skill库（skillspub）是主程序的可扩展“公共技能库”：

- 与内置的 `\@技能`（工具库，存于 `skills/`，由技能工具管理器维护）不同，
  skillspub 面向 **用户 / AI 自由按需调用的通用能力包**，内容完全由用户维护；
- 一次可以调用 **一个或多个** skillspub 技能并互相组合；
- 数据、管理器、开关指令与主程序现有技能体系完全解耦，互相不影响。

---

## 1. 目录结构与数据格式

数据目录：`skillspub/`（可在 `data/config.json → paths.skillspub` 调整）。

**一个公共技能 = 一个独立文件夹「项目」**：文件夹内存放该技能自己的文件
（`SKILL.md` 为详细介绍与执行说明；之后还可按需放入脚本、参考文档等附属文件）。
`catalog.json` **仅作为索引目录**，不存放详细介绍正文。

```
skillspub/
├── catalog.json              ← skill库索引（JSON，唯一索引；不含技能正文）
├── <技能文件夹1>/             ← 每个公共技能一个文件夹（文件夹名 = 技能名净化）
│   └── SKILL.md              ← 该技能的详细介绍与执行说明（真正注入 LLM 的正文）
└── <技能文件夹2>/
    └── SKILL.md
```

`catalog.json` 的每一项公共技能（pub skill）索引字段：

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `name` | 是 | 技能名（唯一；AI 按名称调用） |
| `folder` | 是 | 技能文件夹名（定位 `skillspub/<folder>/SKILL.md`，管理器自动维护） |
| `summary` | 是 | **skill简介**（写入 skillspub 指引，供 AI/用户快速挑选） |
| `category` | 否 | 分类，默认 `通用` |
| `enabled` | 否 | 是否启用，默认 `true`；停用后不出现在指引，也不会被调用 |
| `keywords` | 否 | 调用关键词数组（自动模式的文本匹配降级用） |
| `created_at` / `updated_at` | 否 | 由管理器自动维护 |

> 技能的详细介绍与执行说明（正文）不写入 catalog.json，
> 而是存放在该技能文件夹的 `SKILL.md` 文件中——**管理器保存时同时写索引 + 技能文件**：
>
> - 新建技能：创建 `skillspub/<folder>/` 并写入 `SKILL.md`，同时在索引中登记；
> - 修改技能：覆盖其 `SKILL.md`，并刷新索引条目；
> - 改名技能：整体移动技能文件夹（保留文件夹内的其它附属文件），更新索引；
> - 删除技能：从索引移除，并删除整个技能文件夹（不可撤销）。

索引示例：

```json
{
  "updated": "2026-09-08 12:00:00",
  "count": 3,
  "skills": [
    {
      "name": "信息整理与总结",
      "folder": "信息整理与总结",
      "category": "办公",
      "enabled": true,
      "summary": "把长文本、聊天记录或杂乱资料整理成高信息密度的要点结论与行动清单。",
      "keywords": ["整理", "总结", "要点", "纪要"],
      "created_at": "2026-09-08 12:00:00",
      "updated_at": "2026-09-08 12:00:00"
    }
  ]
}
```

对应技能文件 `skillspub/信息整理与总结/SKILL.md`：

```text
【技能用途】把长段落、聊天记录、会议材料等压缩为要点式结论，便于用户快速决策。
【执行要求】
1. 先区分事实、观点与行动项，分别归纳，不臆造内容；
2. 采用「一句话结论 + 分条要点 + 行动清单」三段式，每条要点尽量独立成行；
...
```

> 首次启动由程序自动创建目录并写入 3 条常见示例技能（`信息整理与总结` /
> `文案润色改写` / `邮件撰写与回复`，每条都含独立文件夹与 `SKILL.md`，可进管理器删除）。
> 仓库另内置一条带附属文件的技能 `tothemoon`（观测条件 / 天象查询）：除了 `SKILL.md`，
> 其 `references/` 下还有 `7timer.md`（晴天钟图 URL 模板与读图细则）与 `cities.md`
> （常用城市经纬度速查）；技能被注入时会一并告知该技能文件夹路径，按需读取即可。
> 旧版 `catalog.json`（正文内联）会在下次打开时自动迁移为上述文件存储。
> 所有写入均为原子替换（先写 `.tmp` 再替换），不会写坏文件。

---

## 2. skill库管理器（skillspub）

设置 →「skill库管理器（skillspub 公共技能库目录管理）」→「打开 skill库管理器」。

设置面板同区域可关闭「允许调用 skillpub」主开关：关闭后 AI 不会主动调用 skill库 技能，
`\@skillspub` / `\@autoskills` 与 #技能名 等调用指令全部失效（默认开启）。

外观与交互与「技能工具管理器」一致（Windows 注册表风格：左侧键树 + 右侧值属性面板，
支持新建 / 修改 / 重命名 / 删除、双击字段弹注册表式编辑框、未保存修改提醒、主题色实时跟随）。
右侧下方可实时看到当前技能对应文件位置（`skillspub\<folder>\SKILL.md`）。

「skill简介」输入框右侧两个一键按钮（后台调用小模型，结果填入后仍需点「保存修改」）：

- **总结功能**：读取下方「详细介绍与执行说明」，整理成符合简介长度（≤200 字符）的一句话简介；
- **翻译总结**：把当前简介（简介为空时读技能说明）总结并翻译为中文一句话。

**保存后立即写入索引 + 技能文件**（见上节），下一条消息即可生效。

---

## 3. 两个内置开关指令（技能工具管理器可启停）

skillspub 功能依托两个**开关型（switch）**内置技能（已内置在技能工具管理器里，
可像思维链/联网搜索一样启用、停用、编辑说明）：

| 开关指令 | 显示名 | 功能 |
| --- | --- | --- |
| `\@skillspub` | skill库Skillspub | 手动模式：允许用户 / AI 自由调用 skill库 中的所有公共技能（一次可多个；「技能广场」为兼容别名） |
| `\@autoskills` | 自动技能AutoSkills | 自动模式：AI 程序自行读取 skill库 指引并自动挑选、组合调用（一次可多个） |

除开关指令外，还可直接在输入框用 **#技能名** 点名调用（如 `#信息整理与总结 帮我整理这段`，
一次可点名多个技能并组合；会弹出技能选择列表，发送后 #技能名 保留在输入框便于连续调用）。

用法与其它开关一致——发送消息时把开关写在消息开头（例如 `\@skillspub 用整理技能处理下面这段`），
发送后开关会保留在输入框，后续消息继续生效；不需要时删掉标签或输入 `\closed` 退出。

两种模式的差别：

- **`\@skillspub`（手动）**：把 skillspub **指引 + 全部技能详细介绍**注入本轮上下文，
  用户点名的技能优先，AI 也可按需自由选用任意技能，适合明确要用某几个技能的场景；
  技能详细介绍来自各技能文件夹的 `SKILL.md` 文件；
- **`\@autoskills`（自动）**：由 AI 程序读取 skillspub 指引（技能名 + skill简介），
  用小模型挑选与本次请求最相关的若干技能并注入其详细介绍后作答；
  小模型不可用时自动降级为关键词文本匹配（对应每个技能的 `keywords`）。

`\@autoskills` 与 `\@skillspub` 同时出现时自动模式优先；
开启任一开关都 **不会** 影响其它 `\@技能` / 场景标签的原有行为。

---

## 4. 校验

原校验脚本（`tools/skillspub_test.py`、`tools/skilltools_test.py`）已随发布清理移除。
校验方式改为手工核对：在 `skilltools.py` 管理器里开关内置技能，确认其它 `\@技能`
与场景标签行为不受影响；并在主界面确认 `#技能名` 弹窗与调用正常。
