# helpset.md — 角色制作与设置完整指南

> 本文档详细说明：如何编写角色卡 JSON、情绪映射 JSON、放置立绘/桌宠图片、
> 设置用户头像与昵称、以及配置群聊。所有文件均为 **UTF-8** 编码。

---

## 一、项目目录总览（· = 可自定义）

```
主文件与主应用\
├── main.py                 主程序入口（python main.py 启动）
├── start.py                启动器（python start.py 启动）
├── helpset.md              本文档
├── roles\                  · 角色库（角色卡目录）
│   ├── group.json            群聊组配置
│   └── 角色名\                每个角色一个文件夹（文件夹名 = 角色名）
│       ├── roles.json        角色卡（核心！）
│       └── emotion.json      情绪关键词映射
├── roles_img\              · 对话立绘（主界面左侧角色形象）
│   └── 角色名\
│       ├── 角色名-.png       默认立绘（必需）
│       └── 角色名-happy.png  情绪立绘（可选）
├── roles_desktop\          · 桌宠动图
│   └── 角色名\
│       └── 角色名-standing.gif  等 7 个动作
├── theme\                  · 主界面背景图片
├── data\
│   ├── config.json          全局配置（API/路径/用户头像/昵称/图标等）
│   └── user_avatar.png       用户头像（可在设置中更换）
└── history\                 记忆与对话存档（自动生成）
```

---

## 二、角色卡 JSON 详细格式（roles.json）

### 2.1 字段说明

| 字段 | 类型 | 是否必需 | 说明 |
|------|------|---------|------|
| `name` | 字符串 | 必需 必需 | 角色名，**必须与文件夹名一致** |
| `display_name` | 字符串 | 可选 | 界面显示名（缺省用 `name`） |
| `personality` | 字符串 | 必需 必需 | 角色性格描述（用于生成主动问候/提醒） |
| `system_prompt` | 字符串 | 必需 必需 | 系统提示词：决定角色说话方式、知识、触发行为 |
| `character description` | 字符串 | 可选 | 角色的详细介绍与背景设定；对话时程序会自动注入系统提示词，角色需按此设定回答 |
| `default_emotion` | 字符串 | 可选 | 无情绪标签时的默认立绘情绪（缺省 `neutral`） |
| `emotion_list` | 数组 | 可选 | 该角色可用的情绪列表 |
| `first_message` | 字符串 | 可选 | 开启新会话时角色自动发送的第一句话 |

### 2.2 标准情绪值

`neutral`（平静）、`happy`（开心）、`sad`（难过）、`angry`（生气）、`surprised`（惊讶）

### 2.3 完整示例

```json
{
  "name": "小林",
  "display_name": "小林酱",
  "personality": "活泼开朗、元气满满、偶尔有点小冒失",
  "system_prompt": "你是小林，一个元气满满的少女助手。说话热情活泼，喜欢用感叹号。"
    "你知识渊博但说话亲切，擅长用比喻解释复杂问题。"
    "回复末尾必须用【】标注情感标签（可选：开心/平静/难过/生气/惊讶），"
    "例如：这个问题很简单啦，我教你！【开心】",
  "default_emotion": "happy",
  "emotion_list": ["neutral", "happy", "sad", "angry", "surprised"],
  "first_message": "你好呀！我是小林，今天也要元气满满哦！【开心】"
}
```

> 注意 `system_prompt` 是角色「灵魂」。建议在其中明确：身份、性格、说话语气、
> 知识边界、回复风格、是否需要【】情感标签。模型会严格遵循它。

---

## 三、情绪映射 JSON（emotion.json）

用于把文本中的中文情绪词 / 关键词映射为程序内部情绪标签（驱动立绘切换）。

```json
{
  "mappings": {
    "neutral":   ["平静", "中性", "平常"],
    "happy":     ["开心", "愉快", "高兴", "兴奋"],
    "sad":       ["难过", "悲伤", "沮丧", "失落"],
    "angry":     ["生气", "愤怒", "不满"],
    "surprised": ["惊讶", "震惊", "意外"]
  },
  "keywords": {
    "happy":     ["哈哈", "太好了", "开心死了"],
    "sad":       ["难过", "伤心", "哭了"],
    "angry":     ["讨厌", "受不了"],
    "surprised": ["什么", "真的吗", "竟然"]
  }
}
```

- `mappings`：回复文本中出现的 `【开心】` 等标签 → 情绪
- `keywords`：用户输入中的关键词 → 情绪（用于快速判断）
- 情绪标签不会直接显示在对话气泡中，只会反映在左侧立绘与情绪标签上

---

## 四、图片放置与命名规范

### 4.1 对话立绘（主界面左侧角色形象）`roles_img\<角色名>\`

| 文件名 | 用途 |
|--------|------|
| `角色名-.png` | **默认立绘（必需）**，无对应情绪图时使用 |
| `角色名-neutral.png` | 平静 |
| `角色名-happy.png` | 开心 |
| `角色名-sad.png` | 难过 |
| `角色名-angry.png` | 生气 |
| `角色名-surprised.png` | 惊讶 |

- 支持 `.png / .jpg / .jpeg / .webp`
- 情绪切换时带 400ms 渐隐过渡

### 4.2 桌宠动图（透明置顶桌宠）`roles_desktop\<角色名>\`

| 文件名 | 动作 |
|--------|------|
| `角色名-standing.gif` | 静止（默认，必需） |
| `角色名-run.gif` | 拖动时 |
| `角色名-say1.gif` | 输入时 |
| `角色名-say2.gif` | 输出时 |
| `角色名-hello.gif` | 定时问候 |
| `角色名-sleep.gif` | 夜间休息提醒 |
| `角色名-work.gif` | 专注模式 |

- 仅支持 `.gif`；全部动作切换为渐隐过渡
- 文件大小需大于 4KB（小于 4KB 会被视为占位图而忽略）

### 4.3 命名示例（角色名 = 小林）

```
roles_img\小林\小林-.png
roles_img\小林\小林-happy.png
roles_img\小林\小林-sad.png
roles_desktop\小林\小林-standing.gif
roles_desktop\小林\小林-run.gif
roles_desktop\小林\小林-hello.gif
```

---

## 五、设置用户（头像与昵称）

### 5.1 界面操作
打开「设置」→「资源路径」分节：
- **用户头像**：文本框直接填图片路径，或点「浏览」选择图片（png/jpg/jpeg/webp/gif/bmp），右侧有实时预览
- **用户昵称**：填写聊天中希望角色如何称呼你（缺省「用户」）

保存后：
- 昵称会应用到对话中**用户气泡的名字**，并作为消息前缀发给模型（如 `小林: 你好`）
- 头像会显示在**用户气泡右侧**的圆形头像上

### 5.2 配置文件（data/config.json）
```json
{
  "user_avatar": "./data/user_avatar.png",
  "ui": { "user_name": "用户" }
}
```
也可以直接改该文件（保持 UTF-8），重启后生效。

---

## 六、任务栏图标（ICO）

打开「设置」→「外观」→「任务栏/应用图标(ICO)」：
- 支持 `.ico`（推荐，多尺寸）或常见图片格式
- 可输入路径或点「浏览」选择
- 配置项：`ui.icon_path`；留空使用内置默认图标
- 保存后同时应用于窗口标题栏图标与系统托盘图标

---

## 七、群聊配置（group.json）

```json
{
  "groups": {
    "讨论组": {
      "members": ["小林", "默认助手"],
      "speaker_hint": "回复必须以 [角色名]: 内容 开头"
    }
  }
}
```

### 字段说明
| 字段 | 说明 |
|------|------|
| `groups` | 群聊组集合，键为组名 |
| `members` | 组内成员角色名（必须是 roles/ 下已存在的角色） |
| `speaker_hint` | 提示模型回复时以「角色名:」开头，便于程序识别发言人 |

### 使用方式
1. 主界面左栏「群聊」下拉选择组名
2. 发送消息后，模型依据各角色卡判定**谁最该回复**，并以 `[角色名]: 内容` 开头
3. 程序解析发言者，自动切换对应角色的立绘与情绪
4. 发言者解析兼容：`[角色A]: 内容`、`角色A：内容`、`角色A 说：内容`、`“内容”——角色A`

---

## 八、添加角色的完整步骤速查

```
1. roles\ 新建文件夹（文件夹名 = 角色名，如 roles\小林\）
2. 写 roles.json（角色卡）+ emotion.json（情绪映射）
3. roles_img\小林\ 放默认立绘 小林-.png + 各情绪立绘
4. roles_desktop\小林\ 放 7 个动作 gif（至少 standing.gif）
5. （可选）在 group.json 中把角色加入群聊
6. 重启应用 → 左栏选择该角色 → 立即生效
```

---

## 九、常见问题

| 问题 | 解决 |
|------|------|
| 角色不显示 | 检查 `roles\角色名\roles.json` 的 `name` 是否与文件夹名一致 |
| 立绘不切换 | 检查 `roles_img\角色名\角色名-<情绪>.png` 命名（注意横杠） |
| 桌宠无动作 | 确认 `roles_desktop\角色名\角色名-<动作>.gif`，且文件 > 4KB |
| 情绪标签出现在气泡里 | 旧版本存档可能含【】，重新对话即隐藏；也可在设置中重置历史 |
| 中文乱码 | 所有 json/图片路径保持 UTF-8 编码保存，不要用 GBK |
| 历史标题是「新会话」 | 第一轮对话后会自动调用 API 生成「角色名-总结」标题，稍等片刻即更新 |
| 如何修改历史标题 | 右侧「历史会话」中对会话条目**右键** →「修改标题」 |